import configs from '../configs/toolbar'
import './toolbar.scss'

class Toolbar {
  constructor({ root }) {
    this.root = root

    this.initElements()
  }

  initElements() {
    const { root } = this
  }
}

export default Toolbar
